project Semi_Space_Gc is
   for Source_Dirs use ("src", ".");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("main.adb", "tests.adb");
   
   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnata", "-gnatwa");
   end Compiler;
end Semi_Space_Gc;
